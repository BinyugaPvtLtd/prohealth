import 'package:flutter/material.dart';

class ContactDocumentScreen extends StatelessWidget {
  const ContactDocumentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Documents'),
    );
  }
}
